public class Main {
    public enum State {
        INIT,
        LOGGING_IN,
        LOGGED_IN,
        EXIT
    }

    public static void main(String[] args) {
        State state = State.INIT;

        while (state != State.EXIT){
            switch (state){
                case INIT:
                    
            }
        }
    }
}
